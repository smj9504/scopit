# Animation improvement plans

Generated by the `improve-animations` skill. Each plan is self-contained — an
executor needs nothing beyond the plan file itself. Run one with
`improve-animations execute <plan>`, or hand the file to any agent.

## Plans

| # | Title | Severity | Status |
| --- | --- | --- | --- |
| [001](001-packing-tool-result-reveal.md) | Bridge the Packing Estimator's async result reveals with a fade+rise entrance | MEDIUM | DONE (executed + reviewed in worktree, PASS WITH NOTES, not yet merged) |

## Execution order

Only one plan exists so far — no ordering constraints yet.

## Dependencies

None yet. Plan 001 touches `frontend/src/styles/global.css` (adds `@keyframes resultReveal` /
`.animate-result-reveal`) — any future plan that wants the same entrance treatment
elsewhere in the app should reuse that class rather than redefining it.

## Known related work not yet planned

Surfaced by an earlier `find-animation-opportunities` sweep of the whole app, not
written up as plans yet:

- Wizard/stepped-flow hard-swaps sharing the same root cause as plan 001, in
  `OnboardingWizard.tsx`, `QuickEstimateTab.tsx`, `PackoutTab.tsx`,
  `CustomerSelector.tsx`, and `ForgotPasswordPage.tsx`.
- Missing press/active feedback on custom buttons and cards (`AppLayout.tsx`
  sidebar toggle, `DashboardPage.tsx` StatCard, `ToolCard.tsx`,
  `LandingPage.tsx`'s local `.btn-primary`/`.btn-secondary`, `.faq-item`).
- `AnnotationLayer.tsx`'s `StampPicker` popover appearing with no
  scale/fade-from-trigger.
- `SignPage.tsx`'s "Document Signed" success state rendered with zero motion.
